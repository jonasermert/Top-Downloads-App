# Contributing

1. Create a focused branch from `main`.
2. Keep changes small and follow the existing feature-oriented structure.
3. Run `dart format .`, `flutter analyze`, and `flutter test`.
4. Open a pull request describing the change and its verification.

Never commit credentials, signing files, generated build output, or local IDE
state.
